{
  "name": "drawscape-outreach",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix web run dev --",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run start -- -p 4200",
    "db:init": "mkdir -p data && npm --prefix web run db:migrate:deploy",
    "db:migrate": "npm --prefix web run db:migrate",
    "db:migrate:deploy": "npm --prefix web run db:migrate:deploy",
    "db:migrate:status": "npm --prefix web run db:migrate:status",
    "db:generate": "npm --prefix web run db:generate",
    "companies": "sqlite3 -header -column data/outreach.sqlite \"SELECT id, name, domain, employee_count, employee_count_range, description, linkedin_company_url FROM companies ORDER BY name;\"",
    "people": "sqlite3 -header -column data/outreach.sqlite \"SELECT p.id, p.profile_key, p.name, p.email, p.phone_number, p.status, p.qualified, p.linkedin_profile_url FROM people p ORDER BY p.name;\"",
    "positions": "sqlite3 -header -column data/outreach.sqlite \"SELECT pos.id, p.name AS person, pos.title, c.name AS company, pos.is_current FROM positions pos JOIN people p ON p.id = pos.person_id JOIN companies c ON c.id = pos.company_id ORDER BY c.name, p.name;\"",
    "web:dev": "npm --prefix web run dev"
  },
  "author": "",
  "license": "ISC",
  "description": "Prospecting workspace for finding companies and people to contact"
}
